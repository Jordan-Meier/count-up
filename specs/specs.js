describe('countUp', function() {
  it("it will count to the given number", function() {
    expect(countUp(10)).to.equal(true);
  });
  // it("it will count to the given number in given increments", function () {
  //
  // })
});
